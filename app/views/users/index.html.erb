<% provide :title, t("page.models.users.index.title") %>
<h1><%= t "page.models.users.index.title" %></h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
