vn:
  # Used in distance_of_time_in_words(), distance_of_time_in_words_to_now(), time_ago_in_words()
  datetime:
    distance_in_words:
      half_a_minute: "nửa phút"
      less_than_x_seconds:
        one:   "khoảng 1 giây"
        other: "khoảng %{count} giây"
      x_seconds:
        one:   "1 giây"
        other: "%{count} giây"
      less_than_x_minutes:
        one:   "khoảng một phút"
        other: "khoảng %{count} phút"
      x_minutes:
        one:   "1 phút"
        other: "%{count} phút"
      about_x_hours:
        one:   "khoảng 1 giờ"
        other: "khoảng %{count} giờ"
      x_days:
        one:   "1 ngày"
        other: "%{count} ngày"
      about_x_months:
        one:   "khoảng 1 tháng"
        other: "khoảng %{ount} tháng"
      x_months:
        one:   "1 tháng"
        other: "%{count} tháng"
      about_x_years:
        one:   "khoảng 1 năm"
        other: "khoảng %{count} năm"
      over_x_years:
        one:   "hơn 1 năm"
        other: "hơn %{count} năm"
      almost_x_years:
        one:   "gần 1 năm"
        other: "gần %{count} năm"
    prompts:
      year:   "Năm"
      month:  "tháng"
      day:    "Ngày"
      hour:   "Giờ"
      minute: "Phút"
      second: "Giây"

  helpers:
    select:
      # Default value for :prompt => true in FormOptionsHelper
      prompt: "Vui lòng chọn"

    # Default translation keys for submit and button FormHelper
    submit:
      create: "Tạo %{model}"
      update: "Cập nhật %{model}"
      submit: "Lưu %{model}"
